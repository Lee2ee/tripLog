services:
  - type: web
    name: triplog-backend
    runtime: java
    buildCommand: ./gradlew bootJar -x test
    startCommand: java -jar build/libs/backend-0.0.1-SNAPSHOT.jar
    healthCheckPath: /api/auth/health
    envVars:
      - key: PORT
        value: 8080
